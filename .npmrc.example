# 说明：真实 `.npmrc` 已在 .gitignore，避免误提交 token。
#
# 本地开发 / 发包：
#   - 使用 `npm login`（凭据写入用户目录 ~/.npmrc，不要复制到本仓库根目录）
#   - CI：`NODE_AUTH_TOKEN` ← GitHub Secret `NPM_TOKEN`（见 .github/workflows/release.yml）
#
# 若团队需要无密钥的共享配置，可在此改名提交为其它文件名，或仅使用 package.json 的 publishConfig。
